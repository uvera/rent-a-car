<button id="locale_button" data-dropdown-toggle="dropdown" class="z-20 flex justify-end w-full text-white bg-accent hover:bg-accent focus:ring-4 focus:outline-none focus:ring-accent rounded-lg px-4 py-2.5 text-center inline-flex items-center dark:bg-accent dark:hover:bg-accent dark:focus:ring-accent" type="button">
  <div class="flex-1 text-left">
    <%= I18n.t("locales.button.title") %>
  </div>
  <svg class="w-4 h-4 ml-2" aria-hidden="true" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
  </svg>
</button>
<!-- Dropdown menu -->
<div id="dropdown" class="z-20 hidden bg-white divide-y divide-gray-100 rounded shadow min-w-fit dark:bg-gray-700 w-64 md:w-32">
  <ul class="py-1 text-sm text-gray-700 dark:text-gray-200 w-full" aria-labelledby="locale_button">
    <% locales = I18n.available_locales %>
    <% locales.each do |locale| %>
      <li class="w-full">
        <%= form_with url: set_locale_locale_path, method: :post, data: { turbo: false }, class: "block w-auto" do |form| %>
          <input type='hidden' value='<%= locale.to_s %>' name="locale">
          <%= form.submit I18n.t("locales.names.#{locale.to_s}"), class: "w-full px-4 py-2 hover:bg-gray-100 dark:hover:bg-gray-600 dark:hover:text-white" %>
        <% end %>
      </li>
    <% end %>
  </ul>
</div>