<div class="flex flex-col">
  <%= render partial: 'search_form' %>
  <div class="flex flex-wrap justify-center gap-5 p-4">
    <% @cars.each do |car| %>
      <%= render partial: 'index_car_view', locals: { car: car } %>
    <% end %>
  </div>
</div>
